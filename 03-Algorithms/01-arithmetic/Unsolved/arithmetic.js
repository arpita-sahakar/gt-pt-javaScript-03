// Fill in the body of each function with the code required

var add = function(num1, num2) {
    var addNum = num1 + num2;
    return(addNum);
};

var subtract = function(num1, num2) {
    var subs = num1 - num2;
    return(subs);
};



var multiply = function(num1, num2) {
    var multi = num1 * num2;
    return(multi);
};

var divide = function(num1, num2) {
    var div = num1 / num2;
    return(div);
};

var result = add(10,12);
console.log(result);

let result1 = subtract(10,12);
console.log(result1);
